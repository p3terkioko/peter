import ErrorDisplay from "@/components/ErrorDisplay";

export default function EnvPage() {
  return (
    <ErrorDisplay 
      code="ENV" 
      message="Restricted Area" 
      quote="Nice try, hackerman. I keep my secrets where you can't reach them." 
    />
  );
}
